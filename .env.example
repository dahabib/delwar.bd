# Database (SQLite)
DATABASE_URL="file:./dev.db"

# Authentication (Auth.js)
AUTH_SECRET="YOUR_AUTH_SECRET"
AUTH_GITHUB_ID="YOUR_GITHUB_ID"
AUTH_GITHUB_SECRET="YOUR_GITHUB_SECRET"

# Allowed email for login (comma-separated)
AUTH_ALLOWED_EMAIL="your-email@example.com"

# SvelteKit
# Public environment variables (prefixed with PUBLIC_) are exposed to the client-side.
# VITE_VERCEL_ENV="development" # Example: set to "production" on Vercel for specific behaviors
